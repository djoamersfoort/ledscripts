<?xml version="1.0"?>
<ruleset>
  <file>djo</file>

  <!-- We just use PSR-2 -->
  <rule ref="PSR2">
    <exclude name="PSR1.Files.SideEffects.FoundWithSymbols"/>
  </rule>

  <!-- Configs -->
  <arg name="colors"/>

</ruleset>
